spring:
  redis:
    host: redis
    port: 6379
    password: ${REDIS_PASSWORD:redispassword}
    ssl: false
    timeout: 2000
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms 